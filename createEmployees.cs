using System;

class createEmployees
{
    public static void create(int amount, Random random, string seed)
    {
        Console.WriteLine(amount + " Employees created with the seed: " + seed);
    }

}